<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-200">
 
    <div class="nav flex items-center justify-between h-12 p-3 bg-gray-800 shadow-md">
        <div>
            <h1 class="text-lg font-bold text-gray-200">Edit Task</h1>
        </div>
        <div class="anchor flex gap-4">
            <a href="/" class="hover:text-gray-400">HOME</a>
        </div>
    </div>


    <div class="edit-container p-6 max-w-lg mx-auto mt-10 bg-gray-800 rounded-md shadow-lg">
        <form action="/editsval/<%= title %>" method="GET">
            <label for="title" class="block text-lg font-semibold mb-2">Title</label>
            <input type="text" id="title" name="title" 
                value="<%= title %>" 
                class="border border-gray-700 bg-gray-900 text-gray-300 p-2 w-full mb-4 rounded focus:outline-none focus:ring focus:ring-blue-500" 
                required />

            <label for="description" class="block text-lg font-semibold mb-2">Content</label>
            <input type="text" id="content" name="description" 
                value="<%= description %>" 
                class="border border-gray-700 bg-gray-900 text-gray-300 p-2 w-full mb-4 rounded focus:outline-none focus:ring focus:ring-blue-500" 
                required />

            <button type="submit" class="mt-4 bg-blue-600 hover:bg-blue-500 text-white p-2 rounded w-full">Update</button>
        </form>
    </div>
</body>
</html>
